#include "simple_shell.h"

/**
 * _strlenconst - count the length of a const string.
 * @s: value string.
 *
 * Return: length of const string.
 */

int _strlenconst(char const *s)
{
	int length;

	for (length = 0; s[length] != '\0'; length++)
	{
	}
	return (length);
}
